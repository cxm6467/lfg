import { ButtonInteraction, ThreadChannel, User } from "discord.js";
import { Document } from "mongoose";

export const addHealerButtonHandler = async (interaction: ButtonInteraction, group: Document, user: User, thread: ThreadChannel) => {
}