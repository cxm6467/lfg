import { ButtonInteraction, ThreadChannel, User } from "discord.js";
import { Document } from "mongoose";

export const addTankButtonHandler = async (interaction: ButtonInteraction, group: Document, user: User, thread: ThreadChannel) => {

}